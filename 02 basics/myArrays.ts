const superHeros: string[]=[]
// const powers: number[]=[]
const powers : Array<number>=[]// just another way of writing the above code 
// const superHeros:Array<string>=[]
superHeros.push("spidy")
powers.push(87)


type User={
    name:string,
    isActive:boolean
}

const allUsers:User[]=[]
allUsers.push({name:"dsfhb", isActive:true})

const MLmodels:number[][]=[
    [2,3,45],
    []
]